import 'package:flutter/material.dart';

class ActualPosition extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    print("Actual Position Page OK");
    return Scaffold(
        backgroundColor: Colors.red,
        body: Text("page Actual Position")
    );
  }
}